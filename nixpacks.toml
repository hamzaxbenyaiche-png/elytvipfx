[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "chromium", "stdenv.cc.cc.lib"]

[phases.install]
cmds = ["pip3 install --break-system-packages -r requirements.txt"]

[start]
cmd = "bash start_bot.sh"
